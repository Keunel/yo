<form method="POST" 

    action="{{ path('app_posts_delete', {'id': post.id}) }}"

    onsubmit="return confirm('Are you sure you want to delete this item?');">

    <input type="hidden" name="_token" value="{{ csrf_token('trucmuche' ~ post.id) }}">

    <button class="btn">Delete</button>
</form>
